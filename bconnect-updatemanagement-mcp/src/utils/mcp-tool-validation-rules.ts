/**
 * MCP Tool Validation Rules — DOMAIN server
 *
 * Add validation rule sets for each tool in this server.
 * See parameter-validator.ts for the ValidationRule type and validateOrThrow usage.
 *
 * Example:
 *
 * import { ValidationRule } from './parameter-validator.js';
 *
 * export const ListDomainRules: ValidationRule[] = [
 *   { field: 'PageSize', type: 'integer', min: 1, max: 1000 },
 * ];
 */

export {};
